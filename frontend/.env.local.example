NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
# CV runs in cv-pipeline on the local GPU machine; the web console stays event-only.
NEXT_PUBLIC_LOCAL_CV_ENABLED=false
