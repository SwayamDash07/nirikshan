import Link from "next/link";
import styles from "../error.module.css";

export default function PrivacyPage() {
  return <main className={styles.page}><section className={styles.card}><p className={styles.code}>NIRIKSHAN PRIVACY</p><h1>Privacy &amp; Data Handling</h1><p>Nirikshan uses local computer vision to estimate crowd density, movement, and risk from configured camera footage.</p><h2>Face blurring</h2><p>Every processed frame passes through local OpenCV face detection and blurring before it can be annotated, displayed, or written as an output. If privacy processing fails, the feed is blocked and marked as failed rather than showing the frame.</p><h2>Retention</h2><p>Raw uploads are deleted after processing by default. Live-feed source files are retained only while that feed is active and deleted when coverage stops. Sanitized processed artifacts and aggregate events follow configurable retention windows.</p><h2>Access</h2><p>Operational footage and processing artifacts require an authenticated administrator or security account. Citizens receive only privacy-safe alerts and aggregate safety guidance. Audit records track uploads, processing, access, deletion, and exports without storing frames, tokens, or face images.</p><h2>Limitations</h2><p>Face detection can miss faces in extreme lighting, occlusion, or unusual camera angles. Nirikshan fails closed when the local detector or frame pipeline errors; operators should verify the privacy status before enabling coverage.</p><Link href="/alerts">Return to Nirikshan</Link></section></main>;
}
